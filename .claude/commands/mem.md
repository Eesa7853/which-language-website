---
description: Save information to the project's persistent memory file
---

Save the following information to `.claude/memory.md` in the project root, so it's available in future sessions.

Info to save: $ARGUMENTS

Steps:
1. Read `.claude/memory.md` first if it exists (create it with a `# Memory` heading and today's date if it doesn't).
2. Add the new info concisely — a short bullet or a couple of lines under a relevant existing heading, or a new heading if it's a new topic. Don't duplicate what's already there; if this updates something already saved, edit that line/section in place instead of appending a near-duplicate.
3. Keep the whole file skimmable — short, plain, no filler.
4. Confirm back to the user in one short sentence what was saved.
